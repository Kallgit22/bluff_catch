name: bluff_catch_functions
description: Server-side Cloud Functions for Bluff Catch (Dart).
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  dart_firebase_admin: ^0.4.1
  functions_framework: ^0.4.3
  shelf: ^1.4.2
  json_annotation: ^4.12.0

dev_dependencies:
  json_serializable: ^6.14.0
  build_runner: ^2.15.0
